#include <stdio.h>
#include <stdlib.h>

/**
 * list_len - returns the number of elements in a linked list
 * @h: pointer to the list_t list
 *
 * Return: number of elements in h
 */

int main(void)
{
int add(int a, int b)
{
	return a + b;
}
int sub(int a, int b)
{
	return a - b;
}
int mul(int a, int b)
{
	return a * b;
}
int div(int a, int b)
{
	if (b == 0)
	{
		printf("erorr: div by zero\n");
		return 0;
	}
	return a / b;
}
int mod(int a, int b)
{
	if (b == 0)
	{
	printf("erorr: div by zero\n");
	return 0;
	}
	return a % b;
}
}
